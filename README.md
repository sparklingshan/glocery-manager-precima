#nodejs and npm is required to build the project

#this project is generated by angular-cli

#Install angular-cli first
sudo npm install -g @angular/cli

#initial project
npm install

#run application
npm start

#run test
npm run test

#browser url
localhost:4200